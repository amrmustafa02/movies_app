import 'package:flutter/material.dart';

class LoginScreen extends StatelessWidget {
  String routeName ="login-screen";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
